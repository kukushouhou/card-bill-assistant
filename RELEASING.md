# 发布规范

每个正式版本都必须在 GitHub 发布页提供完整、详细的中文发布说明，不以自动生成的提交列表代替版本说明。

Codex 执行本项目发布时应自动使用 `.agents/skills/card-bill-assistant-public-release/SKILL.md`，不得等待用户重复提醒更新日志、远端 CI 或多架构镜像验收要求。

## 发布说明要求

1. 以最近一个正式版本标签为基线核对完整差异，同时检查标签之后已经推送的提交和尚未提交的工作区修改。
2. 在 `docs/releases/vX.Y.Z.md` 新建与标签同名的发布说明，并至少包含以下章节：
   - `版本概述`：说明本次发布解决的问题和版本定位。
   - `详细变更`：按功能、接口、部署、文档等类别逐项说明用户可感知的变化。
   - `升级说明`：列出不兼容变化、需要保留的配置、数据库或操作要求；没有迁移也要明确写明。
   - `Docker 镜像`：写明完整镜像地址、同步标签和支持架构。
   - `验证结果`：记录测试、类型检查、构建、Compose 和镜像验证结论。
   - `已知限制`：说明本版本仍存在的功能或部署边界；没有新增限制时也要说明。
3. 发布说明必须给出与上一版本的 GitHub Compare 链接，不能只写“修复若干问题”或直接粘贴 Git 提交标题。
4. README、部署文档、Compose 默认镜像、环境生成脚本以及前后端 `package.json` 和锁文件中的项目版本必须同步更新。
5. 发布前必须完成本地验证，推送后必须等待 `main` 分支 CI 通过，再创建并推送版本标签。
6. 标签触发的发布工作流会检查对应中文说明文件；缺少文件或必要章节时停止发布。多架构镜像发布成功后，工作流才会创建或更新 GitHub Release。

## 发布顺序

1. 完成版本号和发布说明，检查工作区差异及敏感文件。
2. 运行后端测试、前后端类型检查、前端生产构建，以及可用的 Compose/Docker 验证。
3. 提交并推送 `main`，确认 CI 全部通过。
4. 创建带说明的 `vX.Y.Z` 标签并推送。
5. 等待多架构镜像和 GitHub Release 工作流完成。
6. 核对发布页正文、GHCR 标签、`linux/amd64` 与 `linux/arm64` 清单以及仓库状态。
